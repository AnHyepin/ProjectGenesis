<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.green.backend.repository.dao.hyepin.ApplyStatusDao">

    <insert id="insertApply">
        INSERT INTO tbl_apply_status
            (resume_no, application_no, regist_id)
        VALUES
            (#{resumeNo}, #{applicationNo}, #{registId})
    </insert>
    <update id="updatePassCode">
        UPDATE tbl_apply_status
        SET apply_status_gbn_code = #{passCode}
        WHERE application_no = #{applicationNo} AND resume_no = #{resumeNo}
    </update>
</mapper>